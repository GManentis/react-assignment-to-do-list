let userName: string;
//types for example available: string, number, boolean
//....
userName = "foo";

let userAge = 40; //if variable has initial value, the datatype is assigned auto. In this case is number

//Ts can be used in explicit functions:
function myFoo(param1: string, param2:number ) {
    console.log("Guess i will run???");
}

